name = "aristowrap"
version = "0.1.0"
defaultTargets = ["Aristowrap"]

# No Mathlib here: keeps Docker/Podman image builds small. At runtime, `.:/app`
# bind-mounts your real project (often with Mathlib). `verify` builds whatever is
# inside the submitted tarball.

[[lean_lib]]
name = "Aristowrap"
